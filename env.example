# Variables de entorno para Find My Friend

# URL base de la API (solo para producción)
# En desarrollo, la aplicación usará datos mock automáticamente
VITE_API_BASE_URL=http://localhost:8000/api

# Configuración del entorno
# NODE_ENV=development (automático en desarrollo)
# NODE_ENV=production (automático en producción) 